<div align="center">
  <h1> Welcome to WaffleCrisp!</h1>
  <%= image_tag "waffle_boy.png" %>
  <br><br>
  <%= link_to "Create a New Board", new_board_path, class: "btn btn-success" %>
  <br><br>
  <h1> Boards: </h1>
  <% @boards.each do |board|  %>
    <h4><%= link_to board.title, board_path(board.id) %></h4>
  <% end  %>
</div>
